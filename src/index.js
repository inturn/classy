'use strict';

import React from 'react';
import App from './App';
import CodeBlock from './CodeBlock';

const APP = (
  <App>
    <CodeBlock />
  </App>
);

React.render(APP, document.getElementById('root'));
